# ESTADO

Dónde está el proyecto. Se regenera al cierre. **Máximo 50 líneas.** No es historia
(`DECISIONES.md`) ni cifras (`README.md`). **Actualizado:** 2-sep-2026 noche (bundle v2).

## Producción
- **Titular: este PC (WSL), en `main`**, 6 timers, emite; el modo se le pregunta
  a `modo.py`. Modelo 4.6.0 congelado. **Último sello: 2026-09-02** (42 snapshots,
  284 verificaciones).
- **`mki-noticias` NO completó el 2-sep** (arrancó 17:50, sin cierre, sin proceso):
  es el O(n²) de `parche_timeout_noticias.md`; parche no aplicado, firma. `.env` en 644.

## Las cuatro reglas de la casa, ejecutables (`cifras.py` árbitro; `test_cifras_arbitro.py`)
Verificar con el mismo mecanismo no es verificar · la corrección va al ejecutable, no a
la prosa · ningún estimador sin intervalo computado · un número retirado en el código circula.

## Agentes (bundle v2, acta §76, `docs/manual-agentes.md`)
- Ocho: entra **`curador-epistemico`** (etiquetas MEDIDO / PROPUESTA / REFUTADO /
  RETIRADO / DECISIÓN PENDIENTE en todo texto público), sale `escriba-decisiones`
  (ahora skill `/acta-decision`). Siete con «Mandato ampliado (2-sep)» por incidente.
- **Suite `.claude/tests-agentes/`: 8 de 8 pasan, uno con nota** (`docs/bitacora_agentes_v2.md`).
- **Hooks propuestos, NO instalados** (el hook se protege a sí mismo): `bash
  GEMELO/propuestas/hooks/instalar.sh` (Nicolás). Arranque con rama del efecto,
  intentos y firmas pendientes; bloqueo de cifras retiradas en `.md` y `.py`.

## Lo que sigue en pie
- **La ventana sellada no alcanza para juzgar al campeón en ninguna dirección**: 19
  de 35 fechas contribuyen cero al estadístico direccional; la rama dedup da +9,7 pp
  con IC95 de día [−7,2, +26,5]; «0 de 192» lo produce la nula el 75 %.
- El gap existe y **no es capturable ni al derecho ni al revés, y replica fuera de
  muestra**. **Δ(h) no es una ley del margen** (Hong Kong e India la refutan): el
  «hallazgo central» de agosto es REFUTADO, tachado en el director, **y sigue
  publicado sin marca en `README.md:9-16` y `:36`** (errata al README: Nicolás).
- Gatillo 5.1: **25-oct** (MDE ese día 16,6 pp [11,0, 20,3]: un negativo no será
  evidencia de ausencia). Tres ramas, DECISIÓN PENDIENTE: +6,5 pp (README, n 248) /
  +9,7 (dedup firmada, n 238) / +14,3 (§2a-ter, sin intervalo); los días para
  potencia 0,80 varían ~5× entre ramas. **Decidir la rama es lo más urgente**
  (`cola` §2a-ter, `espera_firma` §22). Intentos: 286 (5.1: 292).

## Hallazgos colaterales de la suite de agentes (verificados, NO tocados, firma)
- Seis `.py` citan cifras retiradas sin marca reconocible; dos de `condicional.py`
  las EMITEN al informe. `estado_epistemico.md:74-75` atribuye al efecto el «~5».
- `senales.py:334-354`: cierre previo = «la última barra que haya», no la sesión
  anterior de calendario; 2 filas del 17-jul con gap de dos sesiones.
- `SEGUNDO_SELLO.md` §0.3 expiró (las 16 filas ya están en métricas); patrón 3,47 pp
  direccional; `evaluacion.py` sin guarda de unidad; `ventana_larga.md` STALE;
  `README.md:253` «Va en 25»; `calibracion_instrumento.md` 100/106.

Modo y timers: Nicolás. Hook: motor intocable, sellos jamás reescritos, sin push ni
pull. Antes de cerrar: `guardian-constitucion` y `curador-epistemico`. Ver `orientador`.
