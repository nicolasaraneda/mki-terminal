# Dependencias directas con versiones FIJADAS (Etapa 5.0 WS6 —
# reproducibilidad: pip freeze curado del venv que corre en producción).
# Actualizar una versión es una decisión deliberada, no un efecto lateral.

# --- núcleo del sistema (motor, snapshot, señales) ---
pandas==3.0.3
numpy==2.4.6
yfinance==1.5.1
exchange-calendars==4.13.2

# --- dashboard Streamlit (fallback) ---
streamlit==1.58.0
plotly==6.8.0

# --- noticias + alertas ---
anthropic==0.116.0
feedparser==6.0.12
requests==2.34.2
python-dotenv==1.2.2

# --- API de solo lectura (terminal React) ---
fastapi==0.139.0
uvicorn==0.49.0

# --- desarrollo y verificación ---
pytest==9.1.1
playwright==1.61.0
